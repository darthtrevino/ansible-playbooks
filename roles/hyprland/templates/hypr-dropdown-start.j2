#!/usr/bin/env bash
# Spawns the scratchpad terminal. The class is set explicitly so the window can
# be identified; the flag that does it differs per terminal (wezterm needs
# `start --class`, kitty takes `--class` directly).

exec {{ hyprland_dropdown_command }}
